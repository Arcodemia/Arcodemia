# Log — אתר 1

- **2026-07-29** — נפתחה התיקייה תחת `Personal/`. עוד אין החלטות על נושא או סטאק.
- **2026-07-29** — הוגדר: דף נחיתה לסוכנות שמוכרת דפי נחיתה. קהל = עסקים מקומיים. סטאק = HTML סטטי. המרה = וואטסאפ ראשי + טופס + טלפון. אין תיק עבודות אמיתי, רק דמואים — הדף עצמו הוא ההוכחה.
- **2026-07-30** — מותג: **Aristocraft**. פלטה סגול-ציאן על נייבי כהה. בורר שירותים בשלושה שלבים (חבילה × מטרה × סוג עסק) שבונה הודעת וואטסאפ. נבנתה גרסה 1 מלאה ב-`index.html` — קובץ אחד, בלי תלויות. פרטי קשר עדיין placeholders ב-`CONFIG`.
- **2026-07-30** — החלטה: מחירים לא מוצגים בדף, ולא מתחייבים לזמן אספקה במספר — שניהם נסגרים בשיחה.
- **2026-07-30 — סוף סשן.** גרסה 1 נבנתה ונפתחה בדפדפן, אבל **טרם נבדקה ויזואלית** — אף אחד לא אישר איך זה נראה בפועל. הצעד הבא בסשן הבא: לפתוח את `index.html`, לעבור על המראה (במיוחד ב-375px), ולאסוף תיקונים. שתי החלטות ממתינות לאישור: נוסח הסרת הסיכון, ותווית "עבודת דוגמה" בגלריה.
- **2026-07-30 — גרסה 1 נפסלה על ידי הלקוח: "AI גנרי".** האבחנה: הבעיה לא הייתה במבנה אלא ב**פלטה** — סגול+ציאן על נייבי הוא בדיוק הגרדיאנט שמחוללי AI מייצרים כברירת מחדל, ולכן הדף "הריח" מיוצר גם כשהמבנה היה תקין. **לקח: כשמשהו נראה גנרי, לחפש קודם את הצבע והפונט, לא את הפריסה.**
- **2026-07-30 — עיצוב מחדש (גרסה 2).** ציאן נמחק לגמרי. פלטה חדשה: שחור פחם `#0A090D` · סגול ניאון `#B14BFF` (הצבע הלחיץ היחיד) · זהב בסולם מלבן לברונזה. כותרות עברו ל-Frank Ruhl Libre (סריף עברי) עם גרדיאנט זהב `background-clip:text` שיוצר תאורה מלמעלה. נוספה שכבת גרעין. הוסרו שלוש אנימציות לופ אינסופיות.
- **2026-07-30 — נוסף אפקט פריצה של 4 שניות** בפתיחת הדף: הקוד ברקע מתפענח מגליפים אקראיים, מהיר וזוהר, ואז נרגע. **גילוי:** חייבים לשמר רווחים וירידות שורה בערבול, אחרת רוחב השורות משתנה והפריסה קופצת.
- **2026-07-30 — סעיף "עבודות לדוגמה" הוסר** לבקשת הלקוח. גם הקישור בניווט (הוחלף ב"התהליך") וגם כל ה-CSS.
- **2026-07-30 — סקירת קוד ואבטחה.** נמצא באג אמיתי: `refresh()` השתמש ב-`msg.replace(pick.biz, …)` כשהערך ריק — ו-`"abc".replace("", "X")` מחזיר `"Xabc"`, כלומר כל בחירה חלקית הזריקה `<b></b>` לתחילת ההודעה. תוקן במעבר מ-`innerHTML` לצמתי DOM, מה שגם ביטל את סינק ה-HTML. נוספו: CSP, `noopener noreferrer`, מלכודת ספאם `_gotcha`. **פתוח: `frame-ancestors` לא נאכף מ-`<meta>` — חייב header באירוח.**
- **2026-08-06 — המותג שונה מ-Aristocraft ל-ARCODEMIA.** כולל טלפון, וואטסאפ ואימייל אמיתיים לראשונה.
- **2026-08-06 — עיצוב 2 נזרק. גרסה 3 נבנתה מאפס** לפי רפרנס ויזואלי שהלקוח שלח (דף פינטק כהה עם קריסטלים איריזנטיים). **הקוד הרץ ברקע, הסריקה, אפקט הפריצה, הזהב וה-Frank Ruhl Libre — כולם ירדו.** הוראה מפורשת: "עיצוב חדש לגמרי, תשחרר את הקודם" + "בלי זהב".
- **2026-08-06 — פלטה סופית: שחור טהור `#000` · לבן · סגול ניאון `#B14BFF` (הדגש הלחיץ היחיד).** צבעי הקשת מגיעים אך ורק מהקריסטלים ומשום מקום אחר. אין זהב.
- **2026-08-06 — הקריסטלים נבנו ב-SVG טהור** (פוליגונים + גרדיאנטים איריזנטיים), לא תמונות. שלושה, על אלכסון, מסתובבים 360° ב-`rotate3d` — כלומר מתהפכים במרחב ולא מסתחררים במישור. **זו ההחלטה שהצילה את האפקט:** `rotate()` רגיל נראה כמו מדחף.
- **2026-08-06 — 4 שניות של טעינה חשמלית** החליפו את אפקט הפריצה. כל האפקט ב-CSS תחת `body.charging`, ה-JS רק מדליק ומכבה — כך הכל נשאר על ה-compositor. `transition` על הקשתות מנחית את זה בעדינות במקום לקטוע.
- **2026-08-06 — סעיף "בניית הצעה" (הבורר) הוסר.** שדה "סוג העסק" עבר מ-`<select>` לשדה טקסט חופשי לבקשת הלקוח.
- **2026-08-06 — נוספו שלושה מסמכים משפטיים** כ-`<dialog>` נייטיב: מדיניות פרטיות, תנאי שימוש, הצהרת נגישות. **כולם מכילים `<span class="todo">` צהובים שאסור לעלות איתם לאוויר.** נוסף גם צ'קבוקס הסכמה חובה בטופס.
- **2026-08-06 — הקריסטלים נבנו מחדש (ניסיון 2).** הראשונים נפסלו: "מה זה?". **האבחנה: הם היו פוליגונים שטוחים ברוויית קשת מלאה — נראים כמו סוכריית פלסטיק.** התיקון: (א) גיאומטריה של פריזמה משושה אמיתית — 10 פאות (3 הכתרה + 3 גוף + 3 תחתית + סילואט), כל אחת בגרדיאנט משלה; (ב) **הגוף כמעט שחור** (`#090711`–`#241A3F`) והקשת מופיעה רק בפסי פיזור מעוקלים ב-`mix-blend-mode:screen`. **זה הכלל: זכוכית היא כהה עם אור נשבר, לא צבע מלא.**
- **2026-08-06 — הצורה מוגדרת פעם אחת כ-`<g id="shard">` ב-`<defs>` ומשוכפלת ב-`<use>`.** שלושת הקריסטלים נבדלים ב-`rotate`, `scale`, `hue-rotate` וקצב — אותו נכס, שלוש נוכחויות. הלקוח אישר דמיון לרפרנס, ביקש רק "נגיעות של שוני".
- **2026-08-06 — באג פרספקטיבה שתוקן:** `perspective` ישב על `.crystals` אבל האלמנט שמסתובב (`.cr__body`) הוא **נכד** ולא ילד, ולכן הפרספקטיבה לא הגיעה אליו. הועבר ל-`.cr`. **כלל: perspective חייב לשבת על ההורה הישיר.**
- **2026-08-06 — הוחלט: אין פילטרי SVG על האלמנט המסתובב.** feTurbulence/feGaussianBlur היו מאלצים ריסטור בכל פריים של הסיבוב. הכל גרדיאנטים ומיזוגים; הזוהר הרך עבר ל-`.cr__aura` (CSS) שרק זז ולא מסתובב.
- **2026-08-06 — רצועת המספרים (0 ₪ / 100%) הוסרה** לבקשת הלקוח.
- **2026-08-06 — נוסף וידג'ט נגישות מלא** (8 התאמות: גודל טקסט ×3, ניגודיות, אפור, הדגשת קישורים, עצירת אנימציות, ריווח, פונט קריא, איפוס). נשמר ב-`localStorage` תחת `arcodemia:a11y`. הכפתור בצד ההפוך מהוואטסאפ.
- **2026-08-06 — 🔴 הלקוח מסר שאין לו עדיין עוסק פטור/מורשה.** שלושת המסמכים המשפטיים נכתבו מחדש **בלי אף מציין מקום**: זיהוי לפי מותג + אימייל + טלפון (בלי ח.פ. שלא קיים), סמכות שיפוט "בתי המשפט המוסמכים בישראל" (בלי מחוז), ספק אירוח מתואר גנרית. **נוסף סעיף מפורש שהאתר אינו חנות, אינו גובה תשלום ואין בו סליקה** — זה מה שמחזיק אותו חוקי כשאין עדיין עוסק. **פתוח: רישום כעוסק פטור לפני גביית תשלום ראשון.**
- **2026-08-06 — 🔑 החלפת טכנולוגיה: הקריסטלים עברו מ-SVG ל-WebGL raymarching.** הלקוח דחה גם את גרסת ה-SVG המשופרת. **המסקנה שהייתה צריכה להיאמר מוקדם יותר: SVG לא מסוגל להגיע לרנדר תלת-ממדי עם שבירת אור. פוליגונים שטוחים לא מייצרים פיזור כרומטי, כמה שמשפרים אותם.** עכשיו: SDF של פריזמה משושה + raymarching + שבירה נפרדת לכל ערוץ צבע. בלי ספריות, הכל בקובץ. ה-SVG נשאר כגיבוי אם אין WebGL.
- **2026-08-06 — נבנתה תשתית בדיקה ויזואלית עם Chrome headless.** `mkshot.js` בסקרצ'פאד מייצר עמוד שמרנדר את השיידר ופולט PNG כ-base64 ל-DOM; `--dump-dom` מחזיר אותו. **קריטי: `--screenshot` של Chrome headless מחזיר מסך לבן על קנבס WebGL — חייבים לעבור דרך `toDataURL`.** `mktest.js` מחזיר שגיאות קומפילציה של השיידר. זו הפעם הראשונה בפרויקט שאפשר לראות את התוצאה במקום לנחש.
- **2026-08-06 — ארבע איטרציות על מראה הזכוכית, כל אחת אחרי הסתכלות בפועל:**
  1. `smoothstep` על רעש → **נקודות לבנות כמו קונפטי.** הוחלף במישורי סדק דקים (`fract`+`smoothstep` צר).
  2. הפרש מקדמי שבירה קטן מדי → **אין קשת.** הורחב ל-1.375/1.470/1.605 — מוגזם פיזיקלית, בכוונה.
  3. סביבה מכמה מקורות אור רחבים → **גושי צבע שטוחים.** הוחלפה בפסי ספקטרום.
  4. ספקטרום בלי כיווץ → **פסטל סוכר-כותנה.** `pow(sin(ph*k), 7..12)` מכווץ כל פס לרכס דק, ורוב הכיוונים נשארים שחורים. **זה השלב שהכריע.**
- **2026-08-06 — שני כללים שנלמדו:** (א) `pow` על סינוס הוא מה שהופך "צבע בכל מקום" ל"פס צר" — בלעדיו כל אפקט ספקטרלי יוצא פסטלי; (ב) הפרעת נורמל בתדר גבוה **מכפילה** את תדר הסביבה והתוצאה נראית כמו שיש. הורדה ל-3.4/0.022.
- **2026-08-06 — ביצועים:** ספירות חוסמות אנליטיות לפני כל raymarch (רוב המסך מסתיים שם), תקרת באפר קשיחה של 880px, 36fps, עצירה מלאה כשה-hero לא נראה, והורדת רזולוציה אוטומטית אחרי 40 פריימים איטיים. בליעה לפי חוק בר נותנת עומק. החזרה פנימית מלאה מוסיפה קפיצה שנייה — שם נמצא המבנה הפנימי.

## 2026-08-06 — סשן ארוך אוטונומי (הלקוח ישן)

- **הקריסטלים הושלמו.** נוספו ברקי חשמל **מחוץ** לגבישים לבקשת הלקוח, והצבע **בתוכם** הופחת (`mix(vec3(lum), trans, 0.55)`).
- **🔑 למה הקווים היו מקווקווים:** ב-raymarching, קרן שפוגעת בפאה בזווית משיקית מדלגת מעליה כי הצעד גדול מדי, ופיקסלים סמוכים פוגעים ומחטיאים לסירוגין. התיקון: `t += d * 0.82` + סף שגדל עם המרחק + 110 צעדים.
- **🔑 למה הברקים נראו כמו עשן:** הזיגזג היה בנוי מסינוסים, שמתעגלים. הוחלף ב**גל משולש** (`abs(fract(x)-0.5)*4-1`) שיוצר פינות חדות. גם `w` הוקטן פי 3 לליבה דקה.
- **🔑 באג הטבעת הכהה:** `electric()` חושב **אחרי** בדיקת הספירה החוסמת, ולכן הברקים נחתכו במעגל מושלם. הועבר לפני. גם ההילה קיבלה `smoothstep` שמאפס אותה לפני גבול הספירה.
- **באג גמא:** ענף ההחטאה החזיר צבע בלי טון-מאפינג וגמא, אז ההילה והברקים יצאו כהים פי כמה מהגביש. עכשיו שני הענפים עוברים את אותו טיפול.

### 🔴 הפגם החמור ביותר שנמצא — ותוקן
`.hero__in > *` היה `opacity:0` עם `animation-fill-mode:both`. **אם האנימציה לא רצה — הכותרת הראשית של הדף לא קיימת.** אותו סיכון בכל `.rv`.
**הכלל: אנימציית כניסה לעולם לא יכולה להיות מה שהופך תוכן לנראה.** עכשיו מצב ברירת המחדל נראה; ההסתרה תלויה במחלקות `intro`/`rv-on` שנוספות ב-JS ומוסרות ברשת ביטחון.

### על תשתית הבדיקה
- **headless Chrome אוכף רוחב חלון מינימלי (~500px).** צילום ב-375 מרנדר ב-500 ומקטין — נראה כמו גלישה אופקית שלא קיימת. **בזבזתי על זה זמן. לבדיקת מובייל אמיתית: iframe ברוחב הרצוי, או פרוב שמודד `scrollWidth` מול `clientWidth`.**
- `overflow-x:hidden` מסתיר ראיות מפרוב גלישה — צריך לנטרל אותו זמנית במדידה.

### מה עוד נעשה
- **פונטים:** Heebo מתארח אצלנו, תת-קבוצה לטינית נחתכה (29KB→16KB לכל משקל). אין יותר Google Fonts.
- **CSP עבר ל-HTTP header** ב-`vercel.json`. **הסיבה המעשית: תחת `file://` המקור אטום, `'self'` לא תואם, וה-meta חסם את הפונטים בכל פתיחה מקומית לבדיקה.** נוספו HSTS, Permissions-Policy, COOP/CORP, frame-ancestors.
- **מלכודת הספאם** עברה מ-`-9999px` ל-`clip-path` — מיקום שלילי בלי הורה ממוקם מותח את רוחב הדף.
- **`api/contact.js`** — מייל HTML מעוצב, ולידציה, בריחת HTML, מניעת הזרקת כותרות, הגבלת קצב.
- **הכותרת:** גרדיאנט הקשת על ה-`<em>` הוסר (זה בדיוק ה"AI גנרי" שנפסל בסשן הראשון). הוחלף בהדגשה **דו-גונית**: משפט ראשון ב-62% לבן, המשפט הנושא מסר בלבן מלא. מידת הכותרת הוקטנה מ-6.5rem ל-4.15rem.
- פריסת יצירת הקשר, "יצירת קשר" בתפריט, זוהר על השאלות, JSON-LD, מובייל, `content-visibility`.
- git אותחל, Vercel CLI 58 הותקן גלובלית, `DEPLOY.md` נכתב.

**🔴 ממתין ללקוח (רק הוא יכול):** `npx vercel login` → `npx vercel --prod` → הגדרת `SUPABASE_URL` ו-`SUPABASE_SERVICE_ROLE_KEY`. הכל מפורט ב-[DEPLOY.md](DEPLOY.md).

## 2026-08-06 — הקריסטלים עברו מ-WebGL לתמונה סטטית

הלקוח ביקש לבטל את התלת-ממד בזמן אמת ("מעמיס") ולקבל **תמונה סטטית באיכות גבוהה יותר**.

**הפתרון: לא לזרוק את המנוע — לאפות איתו.** אותו raymarcher רונדר **פעם אחת offline** באיכות שאין שום סיכוי להריץ ב-36fps:
- **פיזור ספקטרלי ב-8 אורכי גל** במקום 3 ערוצי RGB. כל דגימה עם מקדם שבירה משלה (1.372→1.618) ומשקל צבע לפי עקומת רגישות מקורבת. זה מה שנותן את המעברים החלקים במקום שלושה פסים
- **שלוש החזרות פנימיות** במקום אחת
- **4 דגימות אנטי-אליאסינג** לפיקסל
- 200 צעדים, סף הדוק פי 3

**🔑 GPU אמיתי ב-headless:** `--use-angle=d3d11` נותן Intel Iris Xe במקום SwiftShader. **בלי זה הרנדר לא היה מעשי.** גיליתי את זה רק בשלב הזה — כל הרנדרים הקודמים בסשן רצו על תוכנה.

**רנדור בטיילים** (450px) — שיידר שרץ יותר מ-2 שניות ברצף מפעיל TDR reset של הדרייבר וההקשר נופל.

### תוצאה
- 1800×1120 לדסקטופ (82KB webp) · 900×1150 לנייד (53KB webp)
- **הקוד ירד מ-121KB ל-88KB** — כל מנוע ה-WebGL, גיבוי ה-SVG, ה-surge והטעינה החשמלית נמחקו
- **עלות ריצה: אפס.** אין שיידר, אין קנבס, אין לולאת אנימציה
- טעינה ראשונה: **194KB בדסקטופ, 165KB בנייד, 4 בקשות, אפס דומיינים חיצוניים**

### שני כיוונונים שנדרשו אחרי שהסתכלתי
1. **הברקים מילאו את המרכז** והתחרו בכותרת → נוספה מסכה אליפטית (`smoothstep` על `length(uv*vec2(0.62,1.0))`) שמנקה את אזור הטקסט, ומספר הברקים הופחת.
2. **`object-fit:cover` הגדיל את הגבישים** — תמונה ביחס 2.29 בתוך hero ביחס 1.73 נחתכת לרוחב ומתקרבת. תוקן בשניים: רנדר מחדש ביחס 1.6 (כך שהחיתוך אנכי ולא אופקי, וההרכב האופקי נשמר), ו**שכבת החשכה רדיאלית** מעל התמונה שמבטיחה קריאות בכל יחס מסך.

**כלי:** `render.js` בסקרצ'פאד מייצר את התמונות. `node render.js <W> <H> <tile>` ואז Chrome headless עם `--use-angle=d3d11`. המיקומים תלויי-יחס, אז אותו שיידר מייצר גם רחב וגם לאורך.

## 2026-08-06 — הקריסטלים נבנו מחדש: תאורת סטודיו

הלקוח דחה שוב ("אני לא אחזור על זה שוב"). במקום לכוונן עוד פרמטרים, ניתחתי **למה** הרפרנס נראה אחרת.

### 🔑 התובנה: ההבדל היה בתאורה, לא בגביש
הגבישים ברפרנס משקפים **סופטבוקסים מלבניים של סטודיו**. פאה שטוחה שמשקפת מלבן נותנת **כתם לבן גדול עם גבול חד** — ובגבול הזה הפיזור מפריד את הצבעים לקשת. אצלי היו `pow(dot(d,dir), N)` — מקורות נקודתיים שנותנים כתמים רכים ומרוחים, ולכן המראה יצא פלסטי.

`rectLight()` מקרין את הכיוון על מישור האור ומחיל `smoothstep` על שתי הצירים → מלבן עם קצה חד.
⚠️ **כיול:** אור מלבני מכסה זווית מרחבית גדולה בהרבה ממקור נקודתי. העברתי את העוצמות כמו שהן והתמונה יצאה לבנה לגמרי. הירידה: 70→13, 52→9.5.

### עוד שלושה שינויים שנדרשו
- **פרופורציה עבה** — 2.10/0.86 במקום 2.95/0.60. הרפרנס מציג גבישים חסונים, לא מקלות
- **רשת שברים פנימית** — `crackField` (מינימום של שלושה מישורי סינוס מעוותים ברעש), מוערך כאינטגרל לאורך מסלול הקרן בזכוכית. ⚠️ סף 0.075 נתן קורי עכביש; 0.020 נתן את הרשת העדינה של הרפרנס
- **Bloom דו-שכבתי בפוסט** — שתי מריחות (26px ו-7px) ב-`globalCompositeOperation='lighter'` על קנבס 2D. **זה מה שנותן את תחושת הרנדר היקר**, וזה עולה כלום כי הוא נאפה לתמונה

### כיול הרכב
הגבישים העבים תפסו יותר מדי מהמסגרת. `ax()` מ-2.48 ל-3.18 מכפיל יחס המסך → הם יושבים בקצוות וזולגים החוצה, כמו ברפרנס.

**תוצאה:** 49KB רחב · 57KB לאורך. טעינה ראשונה ~161KB דסקטופ.

## 2026-08-17 — זכוכית שקופה + כותרת זוהרת

**הלקוח ביקש להשתמש בקריסטלים מהצילום עצמו.** הסברתי שיצירה נגזרת מתמונה מוגנת נשארת מפרה — סיבוב או שינוי צבע לא הופכים אותה לשלו — וזה **סותר את המטרה שהוא עצמו הציב** ("שלא יוכלו לתבוע אותי"). גם טכנית: הקריסטלים שם ~400px בצילום שטוח בלי אלפא. במקום זה זיהיתי מה עוד שונה ותיקנתי.

### 🔑 למה שלי נראו חלביים והרפרנס שקוף
**האמביינט בסביבה היה גבוה מדי.** קרן שבורה שלא פוגעת באור צריכה להחזיר **שחור** — אז רואים דרך הגביש. אצלי היא החזירה אור סביבה, וכל הגוף נצבע בחלבי.
התיקון: אמביינט מ-0.0015 ל-0.0004, סופטבוקסים **קטנים ובהירים** במקום גדולים ועמומים, בליעה מ-0.16 ל-0.085.
⚠️ **סף:** ירידה ל-`col*=0.44` הפכה אותם לשקופים אבל כמעט בלתי נראים. 0.68 הוא האיזון — שקוף **וגם** בוהק.
- סף השברים מ-0.020 ל-0.011 ועוצמה מ-0.85 ל-2.6 → קווים לבנים מובחנים במקום אובך

### הכותרת
לבן מלא עם שלוש הילות `text-shadow` — צמודה לחידוד, רחבה לזוהר, ורחבה מאוד בסגול קלוש שקושרת אותה לקריסטלים בלי להכניס צבע לאותיות.

### 🔴 לקח תפעולי
**`render2.js` אבד כשהסקרצ'פאד נוקה בין סשנים.** הסקריפט שמייצר את הנכסים שהאתר מציג לא שייך לתיקייה זמנית. הוא נכתב מחדש ויושב עכשיו ב-**`tools/render-crystals.cjs`** בתוך הפרויקט, במעקב git.
⚠️ סיומת `.cjs` ולא `.js` — ה-`package.json` מגדיר `"type":"module"`.

## 2026-08-17 — התלקחויות עדשה במקום ברקי חשמל

הלקוח ביקש "הברקים שיוצאים מהקריסטלים כמו בתמונה". **הסתכלתי שוב ברפרנס: מה שיוצא משם הוא לא ברק חשמלי משונן אלא `lens flare`** — פיצוץ אור בצורת כוכב עם פס אנאמורפי אופקי, בקצוות הגבישים. המילה "ברק" בעברית מכסה את שניהם, וקראתי אותה קודם כחשמל.

`flare()` מרכיב ארבעה חלקים: ליבה גאוסיאנית · **פס אנאמורפי אופקי** · קרני כוכב (`pow(abs(cos(a*3)),26)`) · הילה רכה.
🔑 **הפס האופקי הוא החתימה של עדשה אנאמורפית** — הוא מה שגורם לתמונה להיראות כמו צילום של אובייקט אמיתי ולא כמו ציור.

המוקדים מחושבים מהקודקודים בפועל: `tip(c,m,h,sgn) = c + m*vec3(0,0,sgn*h)`. ⚠️ `mX` ממפה עולם→מקומי, ולכן ההיפוך הוא `M*L` ולא `L*M`.

הזיגזגים הישנים הופחתו ל-30% — רמז בלבד.
⚠️ הפס ההתחלתי (`0.115`) חצה את כל הפריים והפריע לכותרת. `0.050` מכיל אותו סביב הגביש.

## 2026-08-17 — מעבר ל-Next.js 16 (App Router + TypeScript strict)

האתר הסטטי (`index.html` יחיד, 1,583 שורות) הומר לפרויקט Next.js. **המרה 1:1** — העיצוב, הטקסטים, ה-RTL, האנימציות ולוגיקת העסק נשמרו כמות שהם. חריגה מכוונת אחת: תיקון באג הנגישות.

**המבנה:** `app/` (layout, page, globals.css, api/contact/route.ts) · `components/` (11 רכיבים) · `hooks/` (3) · `lib/` (config, whatsapp, types) · `public/img/`.
נקודת שחזור לאתר הסטטי: commit `d1b6ba6`.

### 🔧 הבאג שתוקן
ה-IIFE של `a11y()` הופיע **פעמיים** ב-`index.html`, כלומר שני מאזיני click על אותו כפתור: הראשון פתח את החלונית והשני סגר אותה מיד. **החלונית לא נפתחה לעולם.** עכשיו hook יחיד עם `useState` ו-onClick יחיד. אומת ב-puppeteer מול build הייצור: `display=block` אחרי לחיצה אחת, `none` אחרי שתיים, ההעדפות שורדות רענון, איפוס עובד, Escape סוגר.

### ⚠️ ארבע מלכודות שעלו במעבר — לא לחזור עליהן
1. **`next/font` הוא מאקרו בזמן קומפילציה.** `declarations: [{prop, value: CONST}]` עם הפניה למשתנה — הערך נזרק בשקט והבנייה נופלת על ``missing field `value` ``. חייבים literal.
2. **שתי קריאות `localFont` נפרדות** לעברית וללטינית. `declarations` חל על *כל* ה-@font-face שהקריאה מייצרת, ולכן קריאה אחת הייתה נותנת לכל עשרת הקבצים אותו `unicode-range` ושני קבצים באותו משקל היו דורסים זה את זה.
3. **`process.env.NODE_ENV` לא אמין בטעינת `next.config`.** `'unsafe-eval'` (שנדרש ל-React בפיתוח) **דלף לייצור**. הפתרון: `phase === PHASE_DEVELOPMENT_SERVER`. נתפס ב-`curl -sI` על הכותרות, לא בעין.
4. **`suppressHydrationWarning` נדרש על `<html>`** — סקריפט ה-intro מוסיף `intro`/`rv-on` לפני שה-hydration רץ, ולכן ה-className בלקוח שונה מזה שהשרת שלח.

### מה נבדק בפועל
`npm run build` נקי · `typecheck` אפס שגיאות · `lint` אפס שגיאות · ה-CSP בייצור **זהה מילה במילה** ל-`vercel.json` המקורי · כל קודי השגיאה של ה-API זהים · אפס גלישה אופקית ב-1440 וב-390 · אפס שגיאות קונסולה.

## 2026-08-23 — Phase 2 (חלקי): תשתית Supabase

**⚠️ ההנחיה הגיעה קטועה פעמיים.** מה שנבנה כאן הוא רק החלק שהוגדר במלואו.

**נבנה:**
- `supabase/migrations/` — שתי מיגרציות. ה-DDL **אומת תו-בתו** מול הספסיפיקציה שנמסרה (`diff`), אחרי שבמעבר ל-Next.js תעתוק לפי העין כמעט הרס את `clean()`.
- `leads`: RLS פעיל **בלי אף policy** — רק service-role ניגש. הדפדפן לעולם לא מדבר ישירות עם Supabase בשביל leads.
- `portfolio_items`: RLS + policy ציבורית לקריאה של שורות `published` בלבד.
- `lib/supabase.ts` — לקוח service-role. מייבא `server-only` כדי שייבוא בטעות מרכיב לקוח **ישבור את הבנייה** במקום לדלוף. שם המשתנה בלי `NEXT_PUBLIC_` בכוונה.
- `lib/database.types.ts` · `supabase/config.toml` (`project_id = "arcodemia"`) · `.env.example` + `.env.local` מסונכרנים (6 מפתחות).

**🔴 ההנחיה נקטעה באמצע ה-policy** (`... create policy "public can view published portfolio items" on`). הושלם כ-`for select using (published = true)` — ההשלמה היחידה שמתיישבת עם השם ועם ההערה. **ממתין לאישור.**

**לא נבנה, כי הספסיפיקציה לא הגיעה:** לוגיקת ה-API של בחירת אמצעי הקשר. ארבע התנהגויות נחלקות ומשנות את המימוש מהותית — ראו את השאלות שנמסרו ללקוח. לא נוחשו.

**לא בוצע:** אין remote, אין `supabase link`, אין `db push`, אין פריסה.

## 2026-08-23 — Phase 2: בחירת אמצעי קשר + שכבת Supabase

`mode: 'email' | 'whatsapp'` נוסף לבקשת הטופס. חסר או לא תקין → `'email'`, ההתנהגות שהייתה עד היום.
**שני המצבים עוברים בדיוק את אותה שרשרת** — ולידציה, ניקוי, מלכודת ספאם, הגבלת קצב — ונפרדים רק אחריה.

### 🔑 העיקרון: Supabase הוא שכבת מעקב, לעולם לא חוסם
הערוץ הראשי (קישור וואטסאפ או mailto) הוא שקובע הצלחה או כישלון. תקלה ב-Supabase מתדרדרת ל"הפנייה לא נרשמה" בלוג השרת — **לעולם לא ל"ההודעה של המבקר נכשלה"**.

| | וואטסאפ | מייל |
|---|---|---|
| insert ל-leads | תמיד | תמיד, ראשון |
| פתיחת יישום אצל המבקר | wa.me | mailto: |
| כשל ב-insert | לוג בלבד, ממשיכים | לוג בלבד, ממשיכים |
| קוד תשובה | תמיד 200 | תמיד 200 |

`logLead()` לא זורק ולא מחזיר כשל בשום מסלול — זו ההגנה שמממשת את העיקרון.

בצד הלקוח: במצב וואטסאפ הקישור נפתח **מיד**, באותו tick של אירוע הלחיצה. ⚠️ `window.open` אחרי `await` נחשב לא-מגובה-במחווה וחוסמי חלונות קופצים בולעים אותו.
באג שנתפס בבנייה: במצב וואטסאפ בלי `CONFIG.endpoint` הקישור נפתח פעמיים.

### ⚠️ מלכודת טיפוסים ש-supabase-js מציב
`postgrest-js` דורש שה-schema יתאים ל-`Record<string, ...>`. **`interface` בלי index signature לא מקיים את זה**, וב-TypeScript רק `type` מקבל index signature משתמע. התוצאה היא נפילה שקטה ל-`never` ושגיאה מטעה לגמרי: `"LeadInsert is not assignable to never[]"`.
נדרש להמיר ל-`type` את `Database` **וגם** את `LeadRow`/`LeadInsert`/`PortfolioItemRow`/`PortfolioItemInsert`. בנוסף כל טבלה חייבת `Relationships: []`.
אותרה בעזרת קובץ probe שבודק כל אילוץ בנפרד — לא בניחוש.

### מה נבדק בפועל
מול פרויקט Supabase **אמיתי** שהטבלאות בו עוד לא נוצרו, כלומר כשל אמיתי ולא מדומה:
`whatsapp` + כשל Supabase → **200** · `email` + כשל Supabase → **200** · `mode` חסר/לא תקין → נופל ל-`email` · מלכודת ספאם → 200 שקט · ולידציה עדיין רצה ראשונה.
`build` · `typecheck` · `lint` — נקיים.

## 2026-08-23 — הוסר דיוור חיצוני; המייל הוא mailto:

מצב `email` בטופס פותח `mailto:` אצל המבקר (אותו דפוס כמו וואטסאפ). `/api/contact` רק מאמת ורושם ל-`leads`, ומחזיר 200 לשני המצבים. אין יותר `500 not_configured` / `502 send_failed`.

## 2026-08-24 — קומפוזיט 2D: פיקסלי הגביש הימני במיקום השמאלי

שני סשנים קודמים ניסו ליישר את `pA` (שמאל) ל-`pB` (ימין) בפרמטרי 3D — סיבוב, hue, gain, כיוון env. ההתאמה התקרבה (סגול ~96% מול ~95%, ממוצע זהה) אבל סטיית התקן נשארה שונה. **הסיבה היא לא באג במימוש:** דגימת ה-env-map תלויה בזווית הראייה. אותו משטח בשמאל המסך ובצד ימין דוגם כיוון אחר במפת הסביבה, ולכן 3D לא יכול להגיע לזהות פיקסלים.

הפתרון: אחרי הרנדר, מעתיקים את פיקסלי `pB` האמיתיים למיקום המסך של `pA`. זהות בפנים האטום מובטחת בבנייה.

שכבות מבודדות ב-`tools/render-crystals.cjs` (`noA` / `onlyB` / `onlyA`) בלי לשנות את מסלול הסצנה המלאה. `onlyB` מרונדר עם עמודות נוספות ימינה (אותה מצלמה, בלי מרכוז מחדש) כדי שהגוף לא ייחתך בקצה הקנבס — אחרת החיתוך של הפריים הימני היה הופך לקו אנכי חד בתוך הפריים אחרי ההדבקה. הזזה בשלמים לפי מרכז המסה, אלפא רכה מהבהירות, קומפוזיט premultiplied על שחור, PNG עד ה-WebP האחרון. `tools/composite-left-from-right.cjs` ו-`tools/bake-hero-2d-swap.cjs`.

**אימות lossless מול `right-only` (גוף אטום):** mean/max לכל ערוץ = 0 ב-wide וב-tall. סטיית ה-WebP היא רק דחיסה מאבדת (wide mean ≈ 0.5, max ≈ 40). ב-tall יש הפרש קטן בזוהר הרך באזור שבו ההעתק נפגש עם הגביש הימני המקורי — הקרופ הצר, לא כשל בהעתקה. overlay של Hero (כותרת, CTA) לא נגע.
