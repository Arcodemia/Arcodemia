# אתר 1 — ARCODEMIA

דף נחיתה לסוכנות דו-אנושית שמוכרת **דפי נחיתה בלבד** לבעלי עסקים מקומיים. פרויקט אישי, נוצר 2026-07-29.

- **URL חי:** https://arcodemia.vercel.app
- **סטאק:** Next.js 16 · App Router · TypeScript strict · React 19. **עבר מ-`index.html` יחיד ב-2026-08-17**
- **שפה:** עברית, RTL
- **מטרה נוכחית:** פריסה ל-Vercel. הטופס פותח וואטסאפ או `mailto:` אצל המבקר, והפנייה נרשמת ב-Supabase.

## מבנה הפרויקט

```
app/layout.tsx            metadata, JSON-LD, next/font, סקריפט ה-intro
app/page.tsx              מרכיב את כל החתכים
app/globals.css           כל ה-CSS. 46 טוקנים, תכונות לוגיות ל-RTL
app/api/contact/route.ts  Route Handler שרושם את הפנייה ב-Supabase
app/fonts/                Heebo מתארח אצלנו — אין Google Fonts
components/               רכיב לכל חתך + Dialog + A11yWidget + icons
hooks/                    useA11yPreferences · useReveal · useScrolledPast
lib/                      config · whatsapp · mailto · types
public/img/               תמונות הקריסטלים (רחב + לאורך)
next.config.mjs           כותרות אבטחה + מטמון (החליף את vercel.json)
DEPLOY.md                 ההוראות ללקוח, שלב אחר שלב
```

**פקודות:** `npm run dev` · `npm run build` · `npm run typecheck` · `npm run lint`
**כלים שכבר מותקנים במחשב:** Node 24 · npm 11 · git · Vercel CLI 58 · Python+fonttools (לחיתוך פונטים)

> **המותג היה "Aristocraft" עד 2026-08-06.** אם צצה המילה הזו איפשהו — היא שריד, למחוק.

## הבריף

| שדה | החלטה |
|---|---|
| מותג | **ARCODEMIA** |
| קהל יעד | עסקים מקומיים — מספרות, מוסכים, קליניקות, מסעדות |
| מה מוכרים | דפי נחיתה בלבד. "עוזרים לבעלי עסקים להנגיש את עצמם ברשת" |
| הוכחה | אין תיק עבודות אמיתי. **הדף עצמו הוא ההוכחה** |
| מחירים | לא מוצגים. CTA = "צרו קשר להצעת מחיר" — **למעט חריג אחד מתועד, ראו למטה** |
| בידול | צוות צעיר ונחוש, מבין דיגיטל, מקדם עסקים כאילו הם שלנו |
| זמן אספקה | לא מתחייבים למספר — נאמר בשיחה הראשונה |
| המרה | וואטסאפ ראשי · טלפון · אימייל · טופס · כפתור וואטסאפ צף |
| סטאק | Next.js App Router + TypeScript strict |

**פרטי קשר (אמיתיים, ב-`CONFIG` בראש ה-`<script>`):**
טלפון/וואטסאפ `052-382-2083` (בינלאומי `972523822083`) · אימייל `arcodemia.il@gmail.com`

## 🔴 חריג מדיניות התמחור — מאושר ומתוחם (2026-08-25)

**מדיניות "אין מחירים" נשארת בתוקף בכל האתר.** יש בדיוק **מקום אחד** שבו מוצג מחיר:

> **הטולטיפ של כפתור הוואטסאפ הצף** (`components/WhatsAppFab.tsx`, הקבוע `TIP_TEXT`):
> "שלחו הודעה עכשיו בכדי לקבל דף נחיתה החל מ-850 ₪ !"

זהו **חריג מאושר במפורש על ידי הלקוח, לא היפוך המדיניות.**
❌ אין להעתיק מחיר ל-CTA אחר, לחתך כלשהו, לשאלות הנפוצות או למסמכים.
❌ אין "לתקן" את הטולטיפ בחזרה למצב בלי מחיר — הוא מכוון.
כל שינוי במחיר עצמו נעשה בקבוע אחד: `TIP_TEXT`.

## עיצוב — גרסה 3 (2026-08-06)

**שתי גרסאות קודמות נזרקו.** ראו [log](log.md) לסיבות. הלקוח שלח רפרנס ויזואלי (דף פינטק כהה עם קריסטלים איריזנטיים) וביקש **"עיצוב חדש לגמרי"** — לא תיקון של הקודם.

### מה ירד ולמה — אל תחזיר את זה
- ❌ **זהב** — נפסל מפורשות ("בלי זהב")
- ❌ **Frank Ruhl Libre** / כל סריף — הלקוח רצה סאנס בולד כמו ברפרנס
- ❌ **קוד רץ ברקע + סריקה + אפקט פריצה** — שריד מגרסה 2, לכלך את השחור הנקי
- ❌ **ציאן כצבע ממשק** — מופיע רק בתוך הקריסטלים

### הפלטה
- **שחור טהור `#000`** — הבד
- **לבן** — הכתב. כותרות `font-weight:900`, `letter-spacing:-.035em`, `clamp(2.05rem, 5.6vw, 4.15rem)`. **הכותרת דו-גונית:** משפט ראשון ב-62% לבן, המשפט הנושא מסר בלבן מלא — היררכיה בלי צבע
- **סגול ניאון `#B14BFF`** — הצבע הלחיץ היחיד
- 🔴 **`--coin-red` `#FF3B4E` — מקום אחד בלבד: חלקיקי השקל.**
  היסטוריה שכדאי להכיר לפני שמזיזים את זה. ב-25.8 הוכנס `--danger-red`
  כדגש שני לכל הקשרי האובדן: הביטוי "אתה מפסיד לקוחות עכשיו", ה-✕
  בכרטיסי הכאב וזוהר ה-hover שלהם. ב-26.8 הלקוח ביקש להחזיר את כולם
  לסגול — הטוקן נמחק. באותו יום עצמו הוא ביקש שה**שקלים** יחזרו לאדום,
  ולכן קיים `--coin-red` המצומצם.
  ✅ מותר: `.coin__g` בלבד.
  ❌ **אסור** בטקסט, באיקסים, בזוהר הכרטיסים, ב-CTA, בקישור או בכל
  אלמנט לחיץ. שם הסגול הוא הדגש היחיד.
- **קשת** (`--ir-1`…`--ir-5`) — **אך ורק בתוך הקריסטלים.** לא בממשק, לא בכפתורים
- ירוק וואטסאפ — מותג חיצוני, לא דגש מתחרה

### הקריסטלים — תמונה סטטית שרונדרה מראש ⬅️ המצב הנוכחי

`img/hero-wide.webp` (1800×1120, דסקטופ) · `img/hero-tall.webp` (900×1150, נייד).
בדף: `<picture>` עם `media` — רק החיתוך המתאים נטען. `object-fit:cover` + **שכבת החשכה רדיאלית** ב-`.hero__art::after` שמבטיחה שהכותרת קריאה בכל יחס מסך.

**שני גבישים, ולכל אחד כלל משלו (27.8).** `LAYERS.full` מדליק את `a` ו-`b` בלבד.

- **`a` — השמאלי. חייב להיות שלם.** המיקום והגודל נגזרים מיחס התמונה (`kA`/`mgA`/`uyA`/`halfW` ב-`render-crystals.cjs`), לא מ-`ax()`. `kA` חסום ב-0.70 כי ב-1920x1080 נשארים רק 55% מגובה התמונה אחרי `object-fit:cover`, ו-`uyA` מוריד אותו מתחת לגוש הטקסט **רק** במסגרת צרה — ברוחב שולחני אין לו לאן לזוז והוא חייב לשבת במרכז האנכי.
- **`b` — הימני העליון. נשאר בדיוק כפי שהיה**, כולל החיתוך בקצה. זו בקשה מפורשת של הלקוח, לא פספוס. אל "תתקן" אותו.
- **`c` — הקטן מלמטה. כבוי.** מרכזו יושב מתחת לקצה התמונה, ולכן הוא נראה כמו גביש שמתחיל להיווצר כשגוללים מעט. הלקוח ביקש להסיר.

לפני כל רנדר מחדש: `npm run check:crystals`. הוא מחשב את החיתוך בכל רוחב נפוץ בלי לרנדר.

**איך הן נוצרו — וכך מייצרים חדשות:**
`scratchpad/render.js` הוא אותו raymarcher, מורץ **פעם אחת offline** באיכות בלתי אפשרית בזמן אמת: פיזור ספקטרלי ב-8 אורכי גל (במקום 3 ערוצים), 3 החזרות פנימיות, 4 דגימות AA, 200 צעדים.
```
node render.js 1800 1120 450 hero
chrome --headless=new --use-angle=d3d11 --dump-dom render.html
```
🔑 **`--use-angle=d3d11` נותן GPU אמיתי ב-headless.** בלי זה רץ SwiftShader והרנדר לא מעשי.
🔑 **חובה לרנדר בטיילים** (~450px) — שיידר שרץ >2 שניות ברצף מפעיל TDR reset וההקשר נופל.
המיקומים תלויי-יחס (`ax()`), אז אותו שיידר מייצר גם רחב וגם לאורך.

**עלות ריצה בדף: אפס.** אין שיידר, אין קנבס, אין לולאת אנימציה.
טעינה ראשונה: **194KB דסקטופ / 165KB נייד · 4 בקשות · אפס דומיינים חיצוניים.**

---

### היסטוריה: WebGL בזמן אמת (הוסר 2026-08-06)
*נשמר כי הידע רלוונטי אם אי פעם נחזור לאנימציה — אבל **הקוד נמחק מהדף**, ואין להחזיר אותו בלי בקשה מפורשת.*

**שתי גרסאות SVG נדחו לפני שהובן שזו הטכנולוגיה הלא נכונה.**
🔑 **SVG לא מסוגל לייצר רנדר תלת-ממדי עם שבירת אור.** הקשת בקריסטל אמיתי היא לא ציור — היא התוצאה של אור שנשבר, ולכל אורך גל מקדם שבירה שונה. פוליגונים שטוחים לא יכולים לעשות את זה, כמה שמשפרים אותם. **אם מבקשים שוב "יותר איכותי" — לא לחזור ל-SVG.**

הפתרון: **raymarching של SDF ב-WebGL**, בלי ספריות, הכל בקובץ. הקוד ב-`crystalGL()` בתחתית ה-`<script>`.
- הצורה: פריזמה משושה (`sdHex`) ∩ שישה מישורים משופעים בכל קצה = גביש קוורץ דו-קודקודי
- **הקשת:** כל קרן נשברת שלוש פעמים בנפרד — 1.375 / 1.470 / 1.605. מוגזם פיזיקלית, בכוונה
- החזרה פנימית מלאה מוסיפה קפיצה שנייה. **שם נמצא כל המבנה הפנימי** — בלעדיה הגביש שטוח
- בליעה לפי חוק בר → חלקים עבים כהים יותר → עומק
- שני גבישים, מיקום אופקי נגזר מיחס המסך (אחרת נעלמים בנייד)

❌ גם ה-SVG וגם ה-WebGL **נמחקו מהדף לחלוטין**. אין להם זכר בקוד.

### מה שנלמד על מראה זכוכית — לא לחזור על זה

| הניסיון | התוצאה | התיקון |
|---|---|---|
| `smoothstep` על רעש כ"סדקים" | נקודות לבנות כמו קונפטי | מישורי סדק דקים (`fract` + `smoothstep` צר) |
| הפרש מקדמי שבירה קטן | אין קשת בכלל | להרחיב עד שנראה |
| סביבה מכמה מקורות אור רחבים | גושי צבע שטוחים | פסי ספקטרום |
| ספקטרום בלי כיווץ | פסטל סוכר-כותנה | **`pow(sin(ph*k), 7..12)`** |
| הפרעת נורמל בתדר גבוה | נראה כמו שיש | להוריד ל-3.4 / אמפליטודה 0.022 |

🔑 **הכלל החשוב ביותר:** `pow` על סינוס הוא מה שהופך "צבע בכל מקום" ל"פס צר". בלעדיו כל אפקט ספקטרלי יוצא פסטלי ושטוח.

### ביצועים
ספירות חוסמות אנליטיות לפני כל raymarch (רוב המסך מסתיים שם ולא עולה כלום) · תקרת באפר קשיחה 880px · 36fps · עצירה מלאה כשה-hero לא נראה · הורדת רזולוציה אוטומטית אחרי 40 פריימים איטיים.

### 🔬 תשתית בדיקה ויזואלית — להשתמש בה
בסקרצ'פאד: `mkshot.js` מרנדר את השיידר ופולט PNG כ-base64 ל-DOM, ואז `chrome --headless --dump-dom` מחזיר אותו לקובץ שאפשר להסתכל עליו. `mktest.js` מחזיר שגיאות קומפילציה. `mobile.js` מצלם דרך iframe. `overflow.js` מאתר גלישה אופקית.

שלוש מלכודות שעלו לי זמן — לא לחזור עליהן:
1. **`--screenshot` מחזיר מסך לבן על קנבס WebGL.** חייבים `toDataURL` + `--dump-dom`.
2. **headless אוכף רוחב חלון מינימלי (~500px).** צילום ב-375 מרנדר ב-500 ומקטין, וזה **נראה כמו גלישה אופקית שלא קיימת**. לבדיקת מובייל: iframe ברוחב האמיתי.
3. **`overflow-x:hidden` מסתיר ראיות** מפרוב גלישה — לנטרל זמנית במדידה.

**זו הדרך היחידה בפרויקט לראות תוצאה במקום לנחש. להשתמש בה לפני שמכריזים שמשהו נראה טוב.**

### 🔴 כלל שנשבר פעם אחת ואסור לשבור שוב
**אנימציית כניסה לא יכולה להיות מה שהופך תוכן לנראה.**
`.hero__in > *` היה `opacity:0` עם `fill-mode:both` — כלומר אם האנימציה לא רצה, **הכותרת הראשית של הדף פשוט לא קיימת**. אותו סיכון היה בכל `.rv`.
עכשיו: ברירת המחדל נראית. ההסתרה תלויה במחלקות `intro` / `rv-on` שנוספות ב-JS (ב-`<head>`, לפני הציור הראשון) ומוסרות ברשת ביטחון אחרי 3.2 שניות.

### Phase 3 — שכבת UI/UX (2026-08-25)

- **`.techbg`** עוטף את `#why` + `#process`: רשת קווים (`::before`, ממוסכת ונמוגה), blooms סגולים (`::after`), וחיפושיות. **הכל CSS ו-SVG מוטבע — אפס בייטים נוספים בתעבורה.**
- **סגול, עם חריג אחד: השקלים.** `--danger-red` נמחק. המשפט "אתה מפסיד לקוחות", האיקסים בכרטיסי הכאב והחיפושיות — כולם `--neon`/`--neon-hi`. כפתור הוואטסאפ **בניווט** סגול; הכפתור הצף נשאר ירוק בכוונה. **`--coin-red` הוא היחיד שאדום** — מוגבל אך ורק לחלקיקי ה-`.coin`, לבקשת הלקוח. לא לטקסט, לא לאיקסים, לא לשום דבר לחיץ.
- **הרצועה מתחילה שחורה לגמרי.** אף bloom לא ממוקם מעל 46% אנכית, ולכן הגלילה מתחת לקריסטלים נכנסת לשחור והערבוב הסגול מגיע רק בהמשך. אל תרים את המיקומים.
- **חיפושיות** (`components/Bugs.tsx`) — תשע, בתוך `.techbg` בלבד, **ומתחילות בשורת ארבעת הכרטיסים** ולא בראש הרצועה: מעליהן, באזור הכותרת, אין אף אחת. המרחק נמדד ב-JS (`--bugs-top` + ResizeObserver) כי הוא גובה גוש הכותרת ומשתנה עם רוחב החלון — 363px ב-1440, 339px ב-834, 304px ב-390. כל אחוז קבוע היה שגוי. היו גם בסוף הדף והלקוח ביקש להסיר משם — אל תחזיר אותן לשם. ממבט על, עם ספירלה על הגב, בסגול. כל אחת עם `animation-delay` ומהירות רגליים משלה כדי שלא יצעדו במקהלה. שלוש הולכות שמאלה — `is-rtl` נותן `scaleX(-1)` על הגוף **וגם** `animation-direction:reverse`; אחד בלי השני = חיפושית שצועדת אחורנית. דקורטיביות: `aria-hidden`, `pointer-events:none`, מתחת לכל טקסט.
- **כרטיסי הכאב** — אין בהם איקסים. היו, קודם אדומים ואחר כך סגולים, והלקוח ביקש להוריד (27.8). הדגש עבר לכותרת הכרטיס עצמה: `.pain .card h3` בסגול בהיר, מול גוף אפור.
- **רצף קינטי** ב-`#why`: הכותרת מוארת → אחרי 400ms הביטוי הופך לסגול בהיר → **תשעה** חלקיקי "− ₪" **אדומים** יוצאים מהמילה "לקוחות" ונופלים לתחתית החלון במפל מדורג של כ-4.8 שניות (טבלת `COINS`). פעם אחת לכל צפייה (`unobserve` מיידי).
- **אייקוני התהליך הם SVG מוטבע, לא אמוג׳י** — שישה, `stroke:currentColor`, חסרי צבע במנוחה ונדלקים בריחוף. `:hover` בדסקטופ, `.is-tapped` בנייד.
- **השאלות הנפוצות ירדו** למיקום שמעל ה-footer.

#### 🔴 מלכודות שעלו ביוקר — אל תחזיר אותן

**`contain:paint` שובר `position:fixed`.** `#why` נושא `content-visibility:auto`, שגורר `contain:paint` — **ואב כזה הופך לבלוק המכיל של צאצאי `position:fixed`**. חלקיק השקל מוקם ביחס לחתך ונחת מחוץ למסך. הפתרון: `createPortal` אל `document.body`. כל `position:fixed` בתוך חתך עם `content-visibility` חייב portal.

**עוגן פיזי לאנימציה פיזית.** היצור ברקע הוא `position:absolute` בלי inset inline; בדף RTL המיקום הסטטי הוא השוליים **הימניים**, ו-`bug-crawl` הוא `translateX` פיזי שדוחף משם עוד ימינה — כשזה קרה לעכבישים שקדמו לחיפושיות, **0–1 מתוך 9 היו על המסך.** `.bug` נושא `left:0` פיזי. אל תחליף אותו ב-`inset-inline-start`.

**איך זה נתפס:** לא בעין ולא במספרים — `getBoundingClientRect()` דיווח מספרים שנראו מוסברים על ידי `rotate(-42deg)`. רק צילום מסך הראה שהחלקיק לא שם בכלל. **על מיקום — לצלם, לא להסתמך על rect.**

**`.hero__in::before` הוא הסקרים שמחזיק את הטקסט קריא — אל תמחק אותו.** הגבישים יושבים מאחורי הכותרת, ובלעדיו הכותרת יורדת ל-1.38:1 בנייד והפסקה ל-1.34:1. הוא מחשיך **רק את השטח שמתחת לגוש הטקסט**, ולא את כל התמונה — החשכה גלובלית ב-`.hero__art::after` מכבה גם את הגבישים, שהם כל הפואנטה. יש לו גם רצועה ליניארית בתחתית, בשביל שורת האמון.

⚠️ **הבעיה הזו קדמה לשינוי.** הבסיס שהלקוח חי איתו נכשל ב-9 מדידות. זה לא נוצר כאן — זה תוקן כאן.

⚠️ **ניגודיות נמדדת מתחת לאותיות, לא בתיבת האלמנט.** הגרסה הראשונה של הבדיקה מדדה את התיבה ודיווחה 1.5:1 על פינה ריקה של כותרת ממורכזת. `npm run check:contrast` מצלם פעמיים — עם הטקסט ובלעדיו — ובודק רק פיקסלים שבהם יש אות בפועל.

⚠️ **הפיקסל הגרוע ולא הממוצע.** הממוצע הראה מעל 7:1 בדיוק במקרים שבהם הטקסט נעלם על נצנוץ.

⚠️ `--faint` נותן 4.77:1 על שחור טהור — עובר AA בקושי, בלי מרווח. שורת האמון הועברה ל-`--muted` (6.3:1) בדיוק בגלל זה. אם מוסיפים טקסט מעל תמונה, לא להשתמש ב-`--faint`.

**שלישיית הפטיש.** `HammerIcon` המשוקף, הסימנים ב-`@keyframes fx-hammer` ו-`transform-origin:86% 86%` הם יחידה אחת. שינוי של אחד מהם בלי השניים מחזיר את הפטיש להכות לכיוון ההפוך מהאבן.

### עוד
- `grain` — שכבת feTurbulence ב-data-URI, 4.5%. הורגת את המראה של "גרדיאנט CSS שטוח"
- `frame` — מסגרת קווים דקה, דסקטופ בלבד
- אין שום אנימציית לופ על כפתורים (סימן חובבני + מרוקן סוללה)
- מכבד `prefers-reduced-motion` — הגרעין ואנימציות הכניסה נכבים. התמונה סטטית ממילא

## מבנה הדף

nav → hero (תמונת קריסטלים) → "למה בכלל צריך דף נחיתה" → תהליך 4 שלבים → הסרת סיכון → שאלות נפוצות → צור קשר → footer → 3 dialogs משפטיים + כפתור וואטסאפ צף

**"בורר השירותים" הוסר** (2026-08-06). שדה "סוג העסק" בטופס הוא **טקסט חופשי**, לא `<select>` — לבקשת הלקוח.

## משפטי

שלושה `<dialog>` נייטיביים: `#docPrivacy` · `#docTerms` · `#docA11y`. נפתחים מה-footer ומהצ'קבוקס בטופס.

✅ **אין בהם מציני מקום.** נכתבו מחדש ללקוח שעדיין אין לו עוסק: זיהוי לפי מותג + אימייל + טלפון (בלי ח.פ. שלא קיים), "בתי המשפט המוסמכים בישראל" בלי מחוז, ספק אירוח מתואר גנרית.
**המהלך שמחזיק את זה חוקי:** סעיף מפורש שהאתר אינו חנות, אינו גובה תשלום ואין בו סליקה.
תאריך העדכון מגיע מקבוע `LEGAL_UPDATED` ב-JS, כדי שלא יישאר תאריך מת בדף.

## אבטחה

⚠️ **ה-CSP יושב ב-`next.config.mjs` ולא ב-`<meta>`.** שתי סיבות: header יכול לאכוף `frame-ancestors` ש-meta מתעלם ממנו; ומעשית — תחת `file://` המקור אטום, `'self'` לא תואם, וה-meta **חסם את הפונטים המקומיים בכל פתיחה של הקובץ לבדיקה**.

כותרות שנאכפות: CSP · HSTS (preload) · X-Content-Type-Options · X-Frame-Options · Referrer-Policy · COOP · CORP · Permissions-Policy (הכל מושבת).

- כל הקישורים החיצוניים: `rel="noopener noreferrer"`
- **אין `innerHTML` בשום מקום** — הכל `textContent` / `createElement`
- מלכודת ספאם `_gotcha`, מוסתרת ב-`clip-path`. **לא במיקום שלילי** — `-9999px` בלי הורה ממוקם מותח את רוחב הדף וגורם לגלישה בנייד
- האימייל מוזרק ב-JS מ-`CONFIG` → לא ב-HTML הסטטי, סורקי ספאם לא מוצאים אותו
- אין בקשה לאף שרת חיצוני. הפונטים אצלנו, אין אנליטיקה, אין cookies
- `app/api/contact/route.ts`: ולידציה, בריחת HTML, ניקוי תווי בקרה מכותרות (header injection), הגבלת קצב, `_gotcha` שמחזיר "הצליח" כדי שהבוט לא ילמד
- 🔴 **`clean()` מכיל בתי בקרה גולמיים** (`NUL`–`0x1F`, `DEL`) שאינם נראים בעורך. **אסור לשכתב את השורה "לפי מה שרואים"** — מה שנראה כמו `[

 -]` יסנן רווחים ומקפים במקום תווי בקרה, ויהרוס גם את ההגנה וגם מספרי טלפון
- ⚠️ **`'unsafe-eval'` נדרש ל-React בפיתוח בלבד.** ההבחנה היא לפי `phase` ולא לפי `NODE_ENV` — עם `NODE_ENV` זה דלף לייצור. **לבדוק עם `curl -sI` ולא בעין**

## ביצועים

`content-visibility:auto` לחתכים מתחת לקיפול · פונטים מתארחים אצלנו עם `preload` לשני המשקלים שמעל הקיפול · תת-קבוצה לטינית נחתכה 29KB→16KB · WebGL: ספירות חוסמות, תקרת באפר (880 דסקטופ / 480 נייד), 36fps, עצירה כשלא נראה, הורדת איכות אוטומטית.

## Phase 3 — מתוכנן, לא נבנה

- **דשבורד ניהול** לצפייה בפניות ולעדכון `status` — לא נבנה
- **תצוגת portfolio ציבורית** — הטבלה וה-policy קיימות, אין UI שקורא מהן

## פתוחים

- [ ] 🔴 **הלקוח: להריץ `npx vercel --prod`.** ראו [DEPLOY.md](DEPLOY.md)
- [ ] 🔴 **לפתוח עוסק פטור לפני התשלום הראשון.** האתר עצמו חוקי — הוא לא גובה תשלום ואין בו סליקה, וזה כתוב במפורש בתנאי השימוש
- [ ] 🔴 **עורך דין שיעבור על שלושת המסמכים.** נכתבו כתבניות סבירות בלי מציני מקום, לא כייעוץ משפטי
- [ ] לאשר את נוסח הסרת הסיכון ("לא אהבתם? נפרדים כידידים")
- [ ] לוגו אמיתי (כרגע משולש SVG עם קו סגול)
- [ ] דומיין משלו — ואז לעדכן ב-`robots.txt` את שורת ה-Sitemap
- [ ] לצלם את הדף במכשיר אמיתי — headless לא מרנדר את הקריסטלים ואוכף רוחב מינימלי

## דפים

- [log](log.md) — יומן החלטות
